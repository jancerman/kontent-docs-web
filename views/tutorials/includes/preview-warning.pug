- const host = req.headers.host;
if ((host.indexOf('localhost') > -1 || host.indexOf('preview-') > -1 || host.indexOf('live-dev') > -1) && (UIMessages && UIMessages.banner_for_dev_environment.value !== '<p><br></p>' && UIMessages.banner_for_preview_environment.value !== '<p><br></p>'))
    .preview-warning
        .preview-warning__container
            if (host.indexOf('localhost') > -1 || host.indexOf('-dev') > -1)
                | !{UIMessages.banner_for_dev_environment.value}
            else if (host.indexOf('-master') > -1)
                | !{UIMessages.banner_for_preview_environment.value}
