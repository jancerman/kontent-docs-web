extends ../includes/layout.pug
block content
    main(id='main')
        .article
            .article__content
                | !{content}
                .article__search(error-search)  
    script console.log(`#{req.err.stack.replace(/\\/g, '/')}`);